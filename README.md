<p align="center">
  <img src="https://img.shields.io/badge/FastAPI-009688?style=for-the-badge&logo=fastapi&logoColor=white" alt="FastAPI"/>
  <img src="https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white" alt="Python"/>
  <img src="https://img.shields.io/badge/OpenAI-412991?style=for-the-badge&logo=openai&logoColor=white" alt="OpenAI"/>
</p>

<h1 align="center">📰 Blog Post Generator API</h1>

<p align="center">
  <b>Генерация блог-постов с использованием новостного контекста из NewsAPI и языковой модели OpenAI</b>
</p>

<p align="center">
  <a href="#-описание">Описание</a> •
  <a href="#-возможности">Возможности</a> •
  <a href="#-быстрый-старт">Быстрый старт</a> •
  <a href="#-установка">Установка</a> •
  <a href="#-использование">Использование</a> •
  <a href="#-эндпоинты">Эндпоинты</a>
</p>

---

## 📋 Описание

Сервис представляет собой FastAPI-приложение, которое на основе заданной темы генерирует полноценную статью для блога.

**Как это работает:**
1.  Вы отправляете POST-запрос с темой поста (например, `"тренды в AI"`).
2.  Сервис запрашивает свежие новости по этой теме из **NewsAPI.org**.
3.  Полученные новости используются как контекст для генерации контента через **OpenAI API**.
4.  В результате вы получаете готовый пост, включающий заголовок, SEO-описание и структурированный текст.

---

## ✨ Возможности

| Функция | Описание |
|---|---|
| 📰 **Новостной контекст** | Генерация на основе актуальных новостей для релевантности. |
| 🤖 **OpenAI Power** | Использование продвинутых моделей OpenAI для создания качественного текста. |
| 📝 **Полный пакет** | Автоматическое создание заголовка, meta-описания и тела статьи. |
| ⚙️ **Гибкая настройка** | Возможность управлять языком, количеством новостей и другими параметрами через API. |
| 🚀 **FastAPI** | Асинхронная основа для высокой производительности. |
| 🩺 **Health-чеки** | Встроенные эндпоинты для мониторинга состояния сервиса. |

---

## 🚀 Быстрый старт

```bash
# 1. Клонируйте репозиторий (если еще не сделали)
# git clone ...
# cd ...

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Создайте .env файл из примера
cp env_example .env

# 4. Заполните .env своими API-ключами
# OPENAI_API_KEY=...
# NEWSAPI_KEY=...

# 5. Запустите сервер
uvicorn app:app --reload
```
Сервер будет доступен по адресу `http://127.0.0.1:8000`.

---

## 📦 Установка

### Требования
- **Python** 3.8+
- **NewsAPI ключ** ([newsapi.org](https://newsapi.org/))
- **OpenAI API ключ** ([platform.openai.com/api-keys](https://platform.openai.com/api-keys))

### Шаг 1: Клонирование
```bash
# git clone ...
# cd ...
```

### Шаг 2: Установка зависимостей
Рекомендуется использовать виртуальное окружение.
```bash
# Создание venv (macOS/Linux)
python3 -m venv venv
source venv/bin/activate

# Создание venv (Windows)
python -m venv venv
venv\Scripts\activate

# Установка пакетов
pip install -r requirements.txt
```

### Шаг 3: Настройка .env
Скопируйте файл-пример `env_example` и переименуйте его в `.env`.
```bash
cp env_example .env
```
Заполните файл `.env` своими ключами:
```env
# Обязательные переменные
OPENAI_API_KEY=sk-your-key-here
NEWSAPI_KEY=your-key-here

# Опциональные переменные (можно оставить по умолчанию)
OPENAI_MODEL=gpt-4.1-nano
PORT=8000
# ... и другие
```

---

## 💻 Использование

### Запуск сервера
Для запуска в режиме разработки с автоматической перезагрузкой:
```bash
uvicorn app:app --reload
```

### Пример запроса
Отправьте `POST` запрос на эндпоинт `/generate-post`:
```bash
curl -X POST "http://127.0.0.1:8000/generate-post" \
-H "Content-Type: application/json" \
-d '{"topic": "latest trends in electric vehicles"}'
```

### Остановка
В терминале, где запущен `uvicorn`, нажмите `Ctrl+C`.

---

## ⚙️ Настройки
Все настройки задаются через переменные окружения в файле `.env`.

| Переменная | Описание | По умолчанию |
|---|---|---|
| `OPENAI_API_KEY` | **(Обязательно)** Ключ OpenAI API. | — |
| `NEWSAPI_KEY` | **(Обязательно)** Ключ NewsAPI. | — |
| `PORT` | Порт для запуска сервера. | `8000` |
| `OPENAI_MODEL` | Модель OpenAI для генерации. | `gpt-4.1-nano` |
| `OPENAI_TEMPERATURE` | "Креативность" модели (0.0-1.0). | `0.5` |
| `OPENAI_MAX_OUTPUT_TOKENS` | Макс. токенов в ответе модели. | `2000` |
| `NEWS_LANGUAGE` | Язык новостей (en, de, ru). | `en` |
| `NEWS_PAGE_SIZE` | Кол-во новостей для контекста. | `5` |
| `NEWS_DAYS_BACK` | Глубина поиска новостей в днях. | `7` |
| `REQUEST_TIMEOUT_SEC` | Таймаут для внешних запросов. | `15` |

---

## 🔗 Эндпоинты

| Метод | Путь | Описание |
|---|---|---|
| `POST` | `/generate-post` | **Основной эндпоинт.** Генерирует пост по теме. |
| `GET` | `/` | Корневой эндпоинт. Проверяет, что сервис запущен. |
| `GET` | `/health` | Проверяет, загружена ли конфигурация. |
| `GET` | `/ready` | Проверяет полную готовность сервиса к работе (включая ключи). |
| `GET` | `/heartbeat` | Быстрый ответ, что сервис "жив". |

---

## 📁 Структура проекта
```
.
├── app.py              # Логика FastAPI приложения
├── requirements.txt    # Зависимости Python
├── .env                # Файл с секретными ключами (не в git)
├── env_example         # Пример файла .env
├── .gitignore          # Игнорируемые файлы
└── README.md           # Этот файл